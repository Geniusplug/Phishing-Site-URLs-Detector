services:
  - type: web
    name: race-phish-demo
    runtime: python
    buildCommand: pip install -r requirements-deployment.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health